name: Rust

name: Manual Trigger Example

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Print a message
      run: echo "This is a manual trigger example"
